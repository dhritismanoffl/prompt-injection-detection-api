from typing import List, Tuple, Optional


def calculate_score(results: List[Tuple[bool, float, Optional[str]]]) -> float:
    """
    Public version: scoring logic is abstracted.
    """
    return 0.0